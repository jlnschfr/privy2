# Handle optional dependencies better
optional=true

# Force rebuild of native dependencies
rebuild-bundle=true

# Increase timeout for downloads
timeout=300000

# Better handling of platform-specific packages
target_platform=linux
target_arch=x64

# Disable package-lock shrinkwrap during install
package-lock=false